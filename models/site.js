module.exports = function Site(siteName, region, plotName, owner, ha, offerDesc, status) {
    this.siteName = siteName;
    this.region = region;
    this.plotName = plotName;
    this.owner = owner;
    this.ha = ha;
    this.offerDesc = offerDesc;
    this.status = status;
  };
  